-        if (!button.dataset.hsFairOriginalClass) button.dataset.hsFairOriginalClass = button.className;
         if (inactiveClass) button.className = inactiveClass;
         button.setAttribute('aria-pressed', 'false');
-      } else if (button.dataset.hsFairOriginalClass) {
-        button.className = button.dataset.hsFairOriginalClass;
-        delete button.dataset.hsFairOriginalClass;
-        button.removeAttribute('aria-pressed');
+        button.dataset.hsFairMuted = '1';
+      } else if (button.dataset.hsFairMuted === '1') {
+        if (inactiveClass) button.className = inactiveClass;
+        button.setAttribute('aria-pressed', 'false');
+        delete button.dataset.hsFairMuted;
       }
     }
   }
@@ -617,7 +646,7 @@
   function styleFairFilterButton(button, active) {
     const base = button.dataset.hsFairBaseClass || fairFilterBaseClass(button);
     button.dataset.hsFairBaseClass = base;
-    button.className = `${base} ${active ? 'bg-amber-500/10 text-amber-700 dark:text-amber-300' : 'text-muted-foreground hover:bg-accent bg-transparent'}`.trim();
+    button.className = `${base} ${active ? 'border-0 bg-orange-500/10 text-orange-700 dark:text-orange-300' : 'text-muted-foreground hover:bg-accent bg-transparent'}`.trim();
     button.setAttribute('aria-pressed', String(active));
     button.dataset.state = active ? 'on' : 'off';
   }
@@ -665,7 +694,9 @@
       button.addEventListener('click', event => {
         event.preventDefault();
         event.stopPropagation();
-        setFairFilter(!fairFilterSelected(), true);
+        const enable = !fairFilterSelected();
+        if (enable) clearNativeStatusSelection();
+        setFairFilter(enable, true);
       });
       group.appendChild(button);
     }
diff --git a/tests/dom-navigation.cjs b/tests/dom-navigation.cjs
index 8335ffb..cbd3ff3 100644
--- a/tests/dom-navigation.cjs
+++ b/tests/dom-navigation.cjs
@@ -15,6 +15,6 @@ const {JSDOM}=require('jsdom');const fs=require('fs'),path=require('path'),asser
  await click('[data-service-tab=features]');assert(!w.document.querySelector('#hs-services-root'));w.HSPluginDebug.close();
  const dialog=w.document.createElement('div');dialog.setAttribute('role','dialog');dialog.innerHTML='<form><div class="max-h-[75dvh] overflow-y-auto"><div><input name="remark" value="Host 1"></div><div class="space-y-2"><label>Status</label><div class="flex flex-col gap-2"><div class="flex flex-wrap gap-1"><span class="text-muted-foreground text-sm">No status selected</span></div><button type="button" role="combobox">Select Status</button></div></div></div><button type="submit" disabled>Save</button></form>';w.document.body.appendChild(dialog);await tick();await tick();q('#hs-host-fair-use');assert(!w.document.querySelector('[data-save-fair]'));const statusField=q('[data-hs-fair-status-field]'),combo=statusField.querySelector('[role=combobox]');combo.dispatchEvent(new w.Event('pointerdown',{bubbles:true,cancelable:true}));const portal=w.document.createElement('div');portal.innerHTML='<div role="option" class="focus:bg-accent flex cursor-pointer items-center gap-2 px-4 py-2"><div class="flex w-full items-center gap-3"><button role="checkbox" class="h-4 w-4" data-state="checked" aria-checked="true"><svg data-native-check></svg></button><span class="text-sm font-normal">Active</span></div></div>';w.document.body.appendChild(portal);await tick();const fairOption=q('[data-hs-fair-status-option]');for(const token of portal.querySelector('[role=option]').className.split(/\s+/))assert(fairOption.classList.contains(token));const fairCheck=q('[data-hs-fair-status-option] [data-hs-fair-checkbox]');assert.equal(fairCheck.dataset.state,'unchecked');assert.equal(fairCheck.querySelectorAll('svg').length,0);assert(q('[data-hs-fair-status-option] .hs-fair-hs-tag'));fairOption.dispatchEvent(new w.Event('pointerdown',{bubbles:true,cancelable:true}));await tick();assert.equal(fairCheck.dataset.state,'checked');assert.equal(fairCheck.querySelectorAll('svg').length,1);assert(q('[data-hs-fair-status-badge]').textContent.includes('Fair limited'));assert(q('[data-hs-fair-status-badge] .hs-fair-hs-tag'));assert.equal(dialog.querySelector('button[type=submit]').disabled,false);assert(q('#hs-host-fair-use .hs-fair-enable-row'));assert(q('#hs-host-fair-use .hs-fair-hs-tag'));q('#hs-host-fair-use [data-fair-switch]').click();assert(q('#hs-host-fair-use').dataset.enabled==='1');assert(q('#hs-host-fair-use [data-rate]').textContent.includes('Effective limited speed'));const gb=q('#hs-host-fair-use [data-fair=gb]');gb.value='100';gb.dispatchEvent(new w.Event('input',{bubbles:true}));await w.fetch('/api/host/1',{method:'PUT',body:'{}'});await tick();assert(requests.includes('/api/hs-services/hosts/1/fair-use'));assert(requests.includes('/api/hs-services/hosts/1/fair-status'));assert(q('#hs-host-fair-use').textContent.includes('Shared inbound'));dialog.remove();portal.remove();w.history.pushState({},'', '/dashboard/#/groups');await w.fetch('/api/groups');await tick();const groupDialog=w.document.createElement('div');groupDialog.setAttribute('role','dialog');groupDialog.innerHTML='<form><div class="-mr-4 max-h-[75dvh] space-y-4 overflow-y-auto px-2 pr-4 sm:max-h-[70dvh]"><input name="name" value="VIP"></div><div data-native-footer><button type="submit">Save</button></div></form>';w.document.body.appendChild(groupDialog);await tick();await tick();q('#hs-group-fair-use');const groupScroll=groupDialog.querySelector('.overflow-y-auto');assert(groupScroll.contains(q('#hs-group-fair-use')));assert(!q('#hs-group-fair-use').contains(groupDialog.querySelector('[data-native-footer]')));assert(q('#hs-group-fair-use .hs-fair-enable-row'));assert(q('#hs-group-fair-use .hs-fair-hs-tag'));q('#hs-group-fair-use [data-fair-switch]').click();assert(q('#hs-group-fair-use').dataset.enabled==='1');assert(q('#hs-group-fair-use [data-rate]').textContent.includes('Effective limited speed'));q('#hs-group-fair-use [data-fair=percent]').value='33';q('#hs-group-fair-use [data-fair=percent]').dispatchEvent(new w.Event('input',{bubbles:true}));await w.fetch('/api/group/5',{method:'PUT',body:'{}'});await tick();assert(requests.includes('/api/hs-services/groups/5/fair-use'));assert.equal(q('#hs-group-fair-use [data-fair=mode]').value,'always');groupDialog.remove();
  w.history.pushState({},'', '/dashboard/#/hosts');q('#native-page').innerHTML='<div class="mb-4"><div><input placeholder="Search"></div></div><div class="cursor-default"><div class="group relative"><div>Host 1</div><span>paid</span></div></div><div class="cursor-default"><div class="group relative"><div>Host 2</div><span>free</span></div></div>';await tick();await tick();const groupBar=q('#hs-host-group-bar');assert(groupBar.textContent.includes('VIP 1'));assert(q('#hs-host-group-bar .hs-fair-hs-tag'));const vip=[...groupBar.querySelectorAll('button')].find(b=>b.textContent.includes('VIP'));vip.click();await tick();const host2=[...q('#native-page').querySelectorAll('.cursor-default')].find(e=>e.textContent.includes('Host 2'));assert.equal(host2.style.display,'none');const all=[...groupBar.querySelectorAll('button')].find(b=>b.textContent.startsWith('All'));all.click();await tick();assert.equal(host2.style.display,'');
- w.history.pushState({},'', '/dashboard/#/nodes/cores/1');q('#native-page').innerHTML='<div role="tablist"><button role="tab" aria-selected="true"><svg class="lucide-arrow-up-from-line"></svg>Outbounds</button><button role="tab"><svg class="lucide-arrow-down-to-line"></svg>Inbounds</button></div><div data-core-body></div><div class="sticky"><input id="restart-nodes"><button disabled>Save</button></div>';await tick();await tick();const details=q('#hs-core-tools');details.open=true;await tick();await tick();assert(q('#hs-core-tools textarea[name=profile]'));assert(q('[data-core-body]').contains(details));w.history.pushState({},'', '/dashboard/#/users');q('#native-page').innerHTML='<div data-statuses class="hidden min-w-0 flex-1 items-center gap-1 overflow-x-auto lg:flex">'+['Active','Disabled','Limited','Expired','On Hold'].map(s=>'<button class="inline-flex h-8 shrink-0 items-center rounded-full px-2.5 text-xs font-medium transition-colors text-muted-foreground hover:bg-accent bg-transparent">'+s+'</button>').join('')+'</div><button data-users-refresh aria-label="Refresh users"><svg class="lucide-refresh-cw"></svg></button><table><tbody><tr><td><span>alice</span></td><td><span class="pointer-events-none rounded-full">Active</span></td></tr><tr><td><span>bob</span></td><td><span class="pointer-events-none rounded-full">Active</span></td></tr></tbody></table>';q('[data-users-refresh]').addEventListener('click',()=>w.fetch('/api/users?status=active'));await w.fetch('/api/users?status=active');await tick();await tick();assert(q('[data-hs-fair-badge] .hs-fair-hs-tag'));const fairFilter=q('#hs-fair-filter');assert(fairFilter.textContent.includes('Fair Use'));assert(q('#hs-fair-filter .hs-fair-hs-tag'));fairFilter.click();await tick();await tick();assert(requests.some(r=>r.includes('hs_fair_limited=true')&&!r.includes('status=')));assert(w.location.search.includes('hs_fair_limited=1'));assert.equal(q('#hs-fair-filter').getAttribute('aria-pressed'),'true');assert.equal(q('#hs-fair-filter').dataset.state,'on');q('[data-statuses] button').click();assert(!w.location.search.includes('hs_fair_limited'));
+ w.history.pushState({},'', '/dashboard/#/nodes/cores/1');q('#native-page').innerHTML='<div role="tablist"><button role="tab" aria-selected="true"><svg class="lucide-arrow-up-from-line"></svg>Outbounds</button><button role="tab"><svg class="lucide-arrow-down-to-line"></svg>Inbounds</button></div><div data-core-body></div><div class="sticky"><input id="restart-nodes"><button disabled>Save</button></div>';await tick();await tick();const details=q('#hs-core-tools');details.open=true;await tick();await tick();assert(q('#hs-core-tools textarea[name=profile]'));assert(q('[data-core-body]').contains(details));w.history.pushState({},'', '/dashboard/#/users');q('#native-page').innerHTML='<div data-statuses class="hidden min-w-0 flex-1 items-center gap-1 overflow-x-auto lg:flex">'+['Active','Disabled','Limited','Expired','On Hold'].map((s,i)=>'<button class="inline-flex h-8 shrink-0 items-center rounded-full px-2.5 text-xs font-medium transition-colors '+(i===0?'border-0 bg-emerald-500/10 text-emerald-700 dark:text-emerald-300':'text-muted-foreground hover:bg-accent bg-transparent')+'">'+s+'</button>').join('')+'</div><button data-users-refresh aria-label="Refresh users"><svg class="lucide-refresh-cw"></svg></button><table><tbody><tr><td><span>alice</span></td><td><span class="pointer-events-none rounded-full bg-emerald-500/10 text-emerald-700">Active</span></td></tr><tr><td><span>bob</span></td><td><span class="pointer-events-none rounded-full bg-emerald-500/10 text-emerald-700">Active</span></td></tr></tbody></table>';let nativeStatus='active';const statusButtons=[...q('[data-statuses]').querySelectorAll('button')];const statusValue=b=>b.textContent.trim().toLowerCase().replace('on hold','on_hold');const paintStatuses=()=>statusButtons.forEach(b=>{const on=nativeStatus===statusValue(b);b.className='inline-flex h-8 shrink-0 items-center rounded-full px-2.5 text-xs font-medium transition-colors '+(on?'border-0 bg-emerald-500/10 text-emerald-700 dark:text-emerald-300':'text-muted-foreground hover:bg-accent bg-transparent')});statusButtons.forEach(b=>b.addEventListener('click',()=>{const value=statusValue(b);nativeStatus=nativeStatus===value?'':value;paintStatuses();w.fetch('/api/users'+(nativeStatus?'?status='+nativeStatus:''))}));q('[data-users-refresh]').addEventListener('click',()=>w.fetch('/api/users'+(nativeStatus?'?status='+nativeStatus:'')));await w.fetch('/api/users?status=active');await tick();await tick();assert(q('[data-hs-fair-badge] .hs-fair-hs-tag'));assert.equal(q('[data-hs-fair-badge] .hs-fair-split-native').textContent.trim(),'Active');assert(q('[data-hs-fair-badge] .hs-fair-split-native').className.includes('bg-emerald-500/10'));assert(q('[data-hs-fair-badge] .hs-fair-split-limit').textContent.includes('Fair Use'));const fairFilter=q('#hs-fair-filter');assert(fairFilter.textContent.includes('Fair Use'));assert(q('#hs-fair-filter .hs-fair-hs-tag'));fairFilter.click();await tick();await tick();assert.equal(nativeStatus,'');assert(requests.some(r=>r.includes('hs_fair_limited=true')&&!r.includes('status=')));assert(w.location.search.includes('hs_fair_limited=1'));assert.equal(q('#hs-fair-filter').getAttribute('aria-pressed'),'true');assert.equal(q('#hs-fair-filter').dataset.state,'on');assert(q('#hs-fair-filter').className.includes('bg-orange-500/10'));fairFilter.click();await tick();await tick();assert(!w.location.search.includes('hs_fair_limited'));assert.equal(q('#hs-fair-filter').getAttribute('aria-pressed'),'false');assert.equal(q('#hs-fair-filter').dataset.state,'off');assert.equal(nativeStatus,'');const lastUserRequest=[...requests].reverse().find(r=>new URL(r,w.location.origin).pathname==='/api/users');assert(lastUserRequest);const lastUserUrl=new URL(lastUserRequest,w.location.origin);assert(!lastUserUrl.searchParams.has('status'));assert(!lastUserUrl.searchParams.has('hs_fair_limited'));
  state.features.fair_use.enabled=false;w.dispatchEvent(new w.Event('hs-plugin-feature-changed'));await tick();await tick();assert(!w.document.querySelector('[data-hs-fair-badge]'));assert.equal(q('tbody .pointer-events-none').style.display,'');assert.deepEqual(errors,[]);obs.forEach(o=>o.disconnect());await tick();dom.window.close();console.log('Native UI: sidebar, identical switches, Certbot-only cards, native Host/Group Fair Use and Outbounds placement passed');
 })().catch(e=>{console.error(e);process.exit(1)});
diff --git a/tests/ui-navigation.cjs b/tests/ui-navigation.cjs
index 822f3e7..f95a1d0 100644
--- a/tests/ui-navigation.cjs
+++ b/tests/ui-navigation.cjs
@@ -12,6 +12,6 @@ const {chromium}=require('playwright'),fs=require('fs'),path=require('path'),ass
  await page.locator('#hs-host-fair-use summary').click();assert.equal(await page.locator('[data-save-fair]').count(),0);assert.equal(await page.locator('#hs-host-fair-use .hs-fair-hs-tag').first().innerText(),'HS');assert.equal(await page.locator('#hs-host-fair-use .hs-fair-enable-row').count(),1);assert.equal(await page.locator('#hs-host-fair-use .hs-fair-grid').evaluate(el=>getComputedStyle(el).gridTemplateColumns.trim().split(/\s+/).length),3);await page.locator('#hs-host-fair-use [data-fair-switch]').click();assert.equal(await page.locator('#hs-host-fair-use').getAttribute('data-enabled'),'1');assert((await page.locator('#hs-host-fair-use [data-rate]').innerText()).includes('Effective limited speed'));await page.locator('#hs-host-fair-use [data-fair=gb]').fill('100');await page.locator('#hs-host-fair-use [data-fair=percent]').fill('20');await Promise.all([page.waitForResponse(r=>r.url().endsWith('/hosts/1/fair-use')),page.waitForResponse(r=>r.url().endsWith('/hosts/1/fair-status')),page.evaluate(()=>fetch('/api/host/1',{method:'PUT',headers:{'Content-Type':'application/json'},body:'{}'}))]);assert.equal(fair.threshold_bytes,100e9);assert.equal(fair.speed_percent,20);assert.equal(fairStatus.enabled,true);assert((await page.locator('#hs-host-fair-use').innerText()).includes('Shared inbound'));
  await page.evaluate(()=>{document.querySelector('[role=dialog]').remove();history.pushState({},'', '/dashboard/#/groups');const d=document.createElement('div');d.setAttribute('role','dialog');d.innerHTML='<form><div class="-mr-4 max-h-[75dvh] space-y-4 overflow-y-auto px-2 pr-4 sm:max-h-[70dvh]"><input name="name" value="VIP"></div><div data-native-footer><button type="submit">Save</button></div></form>';d.querySelector('form').addEventListener('submit',e=>{e.preventDefault();fetch('/api/group/5',{method:'PUT',headers:{'Content-Type':'application/json'},body:'{}'})});document.body.appendChild(d);fetch('/api/groups');});await page.waitForSelector('#hs-group-fair-use');assert(await page.locator('.overflow-y-auto').evaluate((scroll,fair)=>scroll.contains(fair),await page.locator('#hs-group-fair-use').elementHandle()));await page.locator('#hs-group-fair-use summary').click();assert.equal(await page.locator('#hs-group-fair-use .hs-fair-hs-tag').first().innerText(),'HS');assert.equal(await page.locator('#hs-group-fair-use .hs-fair-enable-row').count(),1);assert.equal(await page.locator('#hs-group-fair-use .hs-fair-grid').evaluate(el=>getComputedStyle(el).gridTemplateColumns.trim().split(/\s+/).length),2);await page.locator('#hs-group-fair-use [data-fair-switch]').click();assert.equal(await page.locator('#hs-group-fair-use').getAttribute('data-enabled'),'1');assert((await page.locator('#hs-group-fair-use [data-rate]').innerText()).includes('Effective limited speed'));await page.locator('#hs-group-fair-use [data-fair=percent]').fill('33');await Promise.all([page.waitForResponse(r=>r.url().endsWith('/groups/5/fair-use')),page.locator('[data-native-footer] button').click()]);assert.equal(groupFair.mode,'always');assert.equal(groupFair.speed_percent,33);
  await page.evaluate(()=>{document.querySelector('[role=dialog]').remove();document.querySelector('#native-status-portal')?.remove();history.pushState({},'', '/dashboard/#/hosts');document.querySelector('#native-page').innerHTML='<div class="mb-4"><div><input placeholder="Search"></div></div><div class="cursor-default"><div class="group relative"><div>Host 1</div><span>paid</span></div></div><div class="cursor-default"><div class="group relative"><div>Host 2</div><span>free</span></div></div>';});await page.waitForSelector('#hs-host-group-bar');assert((await page.locator('#hs-host-group-bar').innerText()).includes('VIP 1'));const vip=page.locator('#hs-host-group-bar button',{hasText:'VIP'});await vip.click();await page.waitForFunction(()=>[...document.querySelectorAll('.cursor-default')].find(e=>e.textContent.includes('Host 2'))?.style.display==='none');await page.locator('#hs-host-group-bar button',{hasText:'All'}).click();assert.equal(await page.locator('.cursor-default').filter({hasText:'Host 2'}).evaluate(e=>e.style.display),'');
- await page.evaluate(()=>{history.pushState({},'', '/dashboard/#/users');document.querySelector('#native-page').innerHTML='<div data-statuses class="hidden min-w-0 flex-1 items-center gap-1 overflow-x-auto lg:flex">'+['Active','Disabled','Limited','Expired','On Hold'].map(s=>'<button class="inline-flex h-8 shrink-0 items-center rounded-full px-2.5 text-xs font-medium transition-colors text-muted-foreground hover:bg-accent bg-transparent">'+s+'</button>').join('')+'</div><button data-users-refresh aria-label="Refresh users"><svg class="lucide-refresh-cw"></svg></button><table><tbody><tr><td><span>alice</span></td><td><span class="pointer-events-none rounded-full">Active</span></td></tr><tr><td><span>bob</span></td><td><span class="pointer-events-none rounded-full">Active</span></td></tr></tbody></table>';document.querySelector('[data-users-refresh]').addEventListener('click',()=>fetch('/api/users?status=active'));fetch('/api/users?status=active')});await page.waitForSelector('#hs-fair-filter');assert.equal(await page.locator('#hs-fair-filter .hs-fair-hs-tag').innerText(),'HS');assert((await page.locator('#hs-fair-filter').getAttribute('class')).includes('rounded-full'));assert((await page.locator('#hs-fair-filter').getAttribute('class')).includes('h-8'));await page.locator('#hs-fair-filter').click();await page.waitForFunction(()=>new URL(location.href).searchParams.has('hs_fair_limited'));await page.waitForFunction(()=>window.__never===true,{timeout:250}).catch(()=>{});assert(userRequests.some(url=>url.includes('hs_fair_limited=true')&&!url.includes('status=')));assert.equal(await page.locator('#hs-fair-filter').getAttribute('data-state'),'on');await page.locator('[data-statuses] button').first().click();await page.waitForFunction(()=>!new URL(location.href).searchParams.has('hs_fair_limited'));
+ await page.evaluate(()=>{history.pushState({},'', '/dashboard/#/users');document.querySelector('#native-page').innerHTML='<div data-statuses class="hidden min-w-0 flex-1 items-center gap-1 overflow-x-auto lg:flex">'+['Active','Disabled','Limited','Expired','On Hold'].map((s,i)=>'<button class="inline-flex h-8 shrink-0 items-center rounded-full px-2.5 text-xs font-medium transition-colors '+(i===0?'border-0 bg-emerald-500/10 text-emerald-700 dark:text-emerald-300':'text-muted-foreground hover:bg-accent bg-transparent')+'">'+s+'</button>').join('')+'</div><button data-users-refresh aria-label="Refresh users"><svg class="lucide-refresh-cw"></svg></button><table><tbody><tr><td><span>alice</span></td><td><span class="pointer-events-none rounded-full bg-emerald-500/10 text-emerald-700">Active</span></td></tr><tr><td><span>bob</span></td><td><span class="pointer-events-none rounded-full bg-emerald-500/10 text-emerald-700">Active</span></td></tr></tbody></table>';window.__nativeUserStatus='active';const buttons=[...document.querySelectorAll('[data-statuses] button')];const value=b=>b.textContent.trim().toLowerCase().replace('on hold','on_hold');const paint=()=>buttons.forEach(b=>{const on=window.__nativeUserStatus===value(b);b.className='inline-flex h-8 shrink-0 items-center rounded-full px-2.5 text-xs font-medium transition-colors '+(on?'border-0 bg-emerald-500/10 text-emerald-700 dark:text-emerald-300':'text-muted-foreground hover:bg-accent bg-transparent')});buttons.forEach(b=>b.addEventListener('click',()=>{const v=value(b);window.__nativeUserStatus=window.__nativeUserStatus===v?'':v;paint();fetch('/api/users'+(window.__nativeUserStatus?'?status='+window.__nativeUserStatus:''))}));document.querySelector('[data-users-refresh]').addEventListener('click',()=>fetch('/api/users'+(window.__nativeUserStatus?'?status='+window.__nativeUserStatus:'')));fetch('/api/users?status=active')});await page.waitForSelector('#hs-fair-filter');assert.equal(await page.locator('#hs-fair-filter .hs-fair-hs-tag').innerText(),'HS');assert((await page.locator('#hs-fair-filter').getAttribute('class')).includes('rounded-full'));assert((await page.locator('#hs-fair-filter').getAttribute('class')).includes('h-8'));assert.equal(await page.locator('[data-hs-fair-badge] .hs-fair-split-native').innerText(),'Active');assert((await page.locator('[data-hs-fair-badge] .hs-fair-split-native').getAttribute('class')).includes('bg-emerald-500/10'));assert((await page.locator('[data-hs-fair-badge] .hs-fair-split-limit').innerText()).includes('Fair Use'));await page.locator('#hs-fair-filter').click();await page.waitForFunction(()=>new URL(location.href).searchParams.has('hs_fair_limited'));await page.waitForFunction(()=>window.__nativeUserStatus==='');await page.waitForTimeout(120);assert(userRequests.some(url=>url.includes('hs_fair_limited=true')&&!url.includes('status=')));assert.equal(await page.locator('#hs-fair-filter').getAttribute('data-state'),'on');assert((await page.locator('#hs-fair-filter').getAttribute('class')).includes('bg-orange-500/10'));await page.locator('#hs-fair-filter').click();await page.waitForFunction(()=>!new URL(location.href).searchParams.has('hs_fair_limited'));await page.waitForTimeout(120);assert.equal(await page.locator('#hs-fair-filter').getAttribute('data-state'),'off');assert.equal(await page.evaluate(()=>window.__nativeUserStatus),'');const lastUserUrl=new URL(userRequests.at(-1));assert(!lastUserUrl.searchParams.has('status'));assert(!lastUserUrl.searchParams.has('hs_fair_limited'));
  await page.evaluate(()=>{history.pushState({},'', '/dashboard/#/nodes/cores/1');document.querySelector('#native-page').innerHTML='<div role="tablist"><button role="tab" aria-selected="true"><svg class="lucide-arrow-up-from-line"></svg>Outbounds</button><button role="tab"><svg class="lucide-arrow-down-to-line"></svg>Inbounds</button></div><div data-core-body></div>';});await page.locator('#hs-core-tools summary').click();await page.waitForSelector('#hs-core-tools textarea[name=profile]');assert.deepEqual(errors,[]);console.log('Browser regression passed: navigation, Certbot renewal and native Host/Group Fair Use save');await browser.close();
 })().catch(e=>{console.error(e);process.exit(1)});
